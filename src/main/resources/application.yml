server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: git@github.com:hclsaran/springbootmicorservices-cloud-config-server.git
          clone-on-start: true